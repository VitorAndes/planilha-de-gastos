export type ExpenseData = {
  paymentMethod: string;
  expense: string;
};

export type chartDataType = {
  paymentMethod: string;
  expense: number;
};

export type ExpenseType = {
  local: string;
  expense: string;
  tag: string;
  paymentMethod: string;
};
